//Content間は24px空けるのがが理想
import styled from "styled-components";

export const ContentMargin = (props) => {
  return <SContainer></SContainer>;
};

const SContainer = styled.div`
  margin-bottom: 24px;
`;
